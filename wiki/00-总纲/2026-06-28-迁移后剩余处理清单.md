# 迁移后剩余处理清单

记录日期：2026-06-28

## 已完成

1. 云服务器 2026-06-28 迁移包已校验、解压、按用途落位。
2. 原始交易凭证已放入 `raw/01-交割单/原始凭证/trade-original-docs/`。
3. `73wiki-source` 已补齐 `D+20/D+30` 验证周期。
4. 工具默认 RAW 路径已从旧目录切到编号目录：
   - `raw/01-交割单`
   - `raw/02-每日复盘`
   - `raw/03-每日交易计划`
   - `raw/04-市场数据`
   - `raw/05-研报新闻`
   - `raw/06-规则原文`
   - `raw/08-截图`
5. 快速复盘、交割单导入、图片附件、资料导入已改为写入编号 RAW 目录。
6. Lint 与交易计划审计已优先读取编号目录，并兼容旧目录。
7. 文件树已收敛为 RAW / WIKI / 项目配置，并隐藏 `.llm-wiki`、`.obsidian`、`.73wiki`、`.system`、`99-*` 等干扰目录。

## 还需要处理

### P1：数据瘦身审计

目标：先出清单，不直接删除。

待处理目录：

| 目录 | 问题 | 处理方向 |
|---|---|---|
| `wiki/08-信息来源` | 文件最多，很多是资料卡 | 普通资料卡回 RAW 索引，只保留来源规则 |
| `wiki/03-L3个股档案/RAW增量个股卡` | 中间产物过多 | 合并到主档案或事件时间线 |
| `wiki/02-L2方向题材/RAW增量题材卡` | 中间产物过多 | 合并到题材生命周期 |
| `wiki/04-L4交易模式与执行/RAW增量模式卡` | 未验证模式过多 | 未验证进候选，验证后升级正式模式 |
| `wiki/99-待删除审核` | 迁移残留 | 人工确认后删除或归档 |
| `wiki/99-迁移` | 迁移残留 | 保留少量记录，其他归档 |
| `raw/02` | 异常目录 | 已并入 `raw/02-每日复盘`，同名冲突旧版已归档 |

### P2：WIKI 准入规则

目标：禁止资料直接进入 WIKI 主层。

必须补字段：

1. `truth_grade`：S1-S4，判断真不真。
2. `use_grade`：A-D，判断怎么用。
3. `trade_impact`：是否影响作战室、持仓、模式、风险、规则。
4. `evidence_path`：证据路径。
5. `validation_status`：待验证、已验证、证伪、降级。

### P3：交易操作系统首页

目标：打开 73wiki 第一眼看到可执行页面，不是资料列表。

首页应包含：

1. 今日作战室。
2. 当前持仓决策。
3. 今日可做模式。
4. 禁做条件与风险锚。
5. D+验证到期队列。
6. 盘后审计入口。

### P4：模板标准化

目标：让新增页面统一结构，不再自由发挥。

必须标准化：

1. 个股主档案。
2. 持仓决策页。
3. 今日可做模式清单。
4. 作战室。
5. 交易审计页。
6. 错误库条目。
7. 模式升级/降级卡。

### P5：服务器资产二次梳理

已落位但未启用的资产：

1. `Desktop/IFIND-FINANCE-DATA`
2. `Desktop/cron`
3. `Desktop/skills`
4. `Desktop/ths-pipeline-check`
5. `Documents/feishu-codex-bot`
6. `Downloads/small-scripts-only`
7. `.config/zsxq-cli`

这些不应再混入 WIKI；后续按“工具能力是否还需要”单独审计。

## 下一步建议

下一步先做 P1：写一个只读审计脚本，扫描 `wiki/73神话/wiki`，输出以下清单：

1. 应保留的稳定规则页。
2. 应合并的 RAW 增量卡。
3. 应迁回 RAW 索引的普通资料卡。
4. 应人工确认删除的迁移残留。
5. 重复文件、乱码文件、空文件。

脚本第一版只输出 `.llm-wiki/migration-triage/` 报告，不执行移动或删除。
