# 2026-07-05 Mac-first 系统重建记录

## 结论

本次不是继续修补旧 Windows/服务器逻辑，而是把活动配置重建为 Mac 主控口径：

- Mac 负责主控、自动抓取、AI 上下文、Codex 分析产物、WIKI 发布、D+验证和系统审计。
- 龙虾/老虎统一定义为外部事实层，只写 RAW，不写 WIKI，不做最终交易结论。
- Windows 盘符和旧服务器只允许出现在隔离区、历史记录或必要说明里，不能作为当前任务入口。

## 已重建的活动配置

1. `73wiki.system.json`
   - 版本升为 2。
   - `monitorDirs` 改为当前编号 RAW 目录。
   - `analytics.tradeSourceDirs` 改为 `raw/01-交割单`、`raw/02-每日复盘`、`raw/10-飞书交易沟通`。
   - `ignoredPathIncludes` 改为 Mac/POSIX 口径，排除 `.conflicts`、嵌套旧副本、虚拟环境、`raw/raw`、`wiki/wiki` 等噪音源。

2. `73wiki.config.json`
   - 自动摄入 watch 补齐 `raw/08-截图`、`raw/09-短线知识`、`raw/10-飞书交易沟通`、`raw/11-Codex分析产物`。
   - Codex 权限补齐 01-11 RAW。
   - TdxClaw/龙虾权限改为外部事实层，只写 RAW。

3. `.system/data-interface-registry.json`
   - 去掉“Windows本地任务”表述。
   - 改成“外部事实层任务（龙虾/老虎），Mac验收和二次分析”。
   - WeRSS 改为不依赖任何远端旧 WeRSS 入口。

4. `raw/07-系统脚本/codex_run_daily_closed_loop.py`
   - 回传目录从 `回传Windows` 改为 `回传外部事实层`。
   - 手工复制说明去掉硬编码盘符，只保留“复制到对端 Wiki 根目录”的规则。

5. 龙虾/老虎规则
   - `给龙虾-每日复盘数据硬要求.md` 改成事实源模式：龙虾交原始涨停原因、盘口、板块、资金等事实；6维深度分析由 Codex Pro 完成。
   - `收盘事实层六模块采集验收规则.md` 改成外部事实层口径。
   - 回传模板改为外部事实层，不再绑定 Windows。

## 已隔离或降级

- 活动脚本目录里的旧 `.cmd/.ps1/.bat` 已清零。
- `raw/07-系统脚本/legacy-tools` 和 Windows 端口死锁历史报告已移入 `.conflicts/mac-migration-quarantine-20260704/legacy-active-raw07/`。
- `.system/codex-raw-watch-queue.indexed-*` 和 6月迁移清单已移入 `.conflicts/mac-migration-quarantine-20260704/system-history-indexes/`。
- `.system/ingest-registry.jsonl` 已备份，并机械重写旧路径到当前编号目录。
- 空旧目录 `raw/交割单`、`raw/日复盘` 已移除。
- `.llm-wiki/.llm-wiki` 和 `data/data` 已移入 `.conflicts/mac-migration-quarantine-20260704/nested-runtime/`，活动根目录不再保留嵌套副本。
- `.system/logs` 已接入自动日志轮转：超过 5MB 压缩，保留 7 个归档。

## 验证结果

- JSON 配置校验通过：
  - `73wiki.system.json`
  - `73wiki.config.json`
  - `.system/data-interface-registry.json`
- Mac 迁移残留审计：
  - `必须修复: []`
  - `Windows侧工具文件: []`
- 活动脚本目录：
  - `.system`、`raw/07-系统脚本`、`wiki/10-系统配置/scripts` 无活动 `.ps1/.cmd/.bat` 入口。
- WeKnora Lite：
  - 进程存在。
  - `http://127.0.0.1:8080/` 返回 200。
- 数据接口健康报告：
  - 已接入周末/用户提供/备用接口/周日任务分状态，避免长期运行误报。
  - 已接入 Syncthing 状态摘要。
- 数据接口健康报告已生成：
  - `.system/data-interface-health.json`
  - `wiki/09-统计与进化/2026-07-05-数据接口运行总控报告.md`

## 仍需后续处理

安全 Wiki lint 仍有 782 个失效链接。这个不是 Mac 迁移问题，而是历史 Wiki 内容的链接债务，后续应按目录逐批修：

1. 先修 `wiki/00-总纲`、`wiki/09-统计与进化`、`wiki/07-作战室` 的交易主链。
2. 再修 `wiki/05-错误库` 和 `wiki/04-L4交易模式与执行` 的规则链。
3. 最后处理 `wiki/08-信息来源`、RAW增量题材卡和历史自动生成页。

全库深度 lint 仍不建议直接跑；必须走 `.wikiignore` 和安全 lint 口径，避免 RAW、缓存、隔离区和历史自动生成页拖死应用。
