# 2026-07-04 Mac迁移收尾执行记录

## 结论

Mac 当前主链路已经不再依赖 Windows `.ps1/.cmd/.bat` 入口。

活动目录检查结果：

- `.system`：无 `.ps1/.cmd/.bat`
- `raw/07-系统脚本`：无 `.ps1/.cmd/.bat`
- `wiki/10-系统配置/scripts`：无 `.ps1/.cmd/.bat`
- LaunchAgent：无 Windows 路径、无 PowerShell 入口
- 数据接口注册表：JSON 正常
- Mac 迁移残留审计：`必须修复: []`

## 本次已处理

1. 隔离 Windows 旧脚本。
   - 隔离目录：`.conflicts/mac-migration-quarantine-20260704/windows-scripts`
   - 内容包括 `.system/.system` 嵌套旧副本、旧 PowerShell 脚本、旧 `.cmd/.ps1` 启动器、旧 RAW watcher。

2. 移走根目录 Windows 一次性脚本。
   - 原根目录：`extract_l1.py`
   - 迁移后：`raw/07-系统脚本/legacy-tools/extract_l1_windows_once.py`

3. 建立 Mac 总启动脚本。
   - `raw/07-系统脚本/start_73wiki_mac_services.sh`
   - 用于统一加载/重启 73wiki 的 Mac LaunchAgent。

4. 去掉活动脚本对旧市场目录的兼容。
   - `check-longxia-review-quality.py` 只读 `raw/04-市场数据`
   - `codex_ths_hotlist_movers.py` 只读 `raw/04-市场数据`
   - 不再把 `raw/04-行情市场数据` 当有效主路径。

5. 建立全库 lint 降噪规则。
   - `.wikiignore`
   - `wiki/10-系统配置/Wiki清理与Lint降噪规则.md`

6. 建立可重复审计脚本。
   - `raw/07-系统脚本/codex_audit_mac_migration_residue.py`
   - 输出：
     - `.system/mac-migration-residue-audit.json`
     - `wiki/09-统计与进化/2026-07-04-Mac迁移残留与Wiki结构审计.md`

## 当前 Mac 自动任务状态

已加载：

- `com.73wiki.cloud-data-connectors`
- `com.73wiki.data-interface-health`
- `com.73wiki.eastmoney-market-snapshot`
- `com.73wiki.evening-stock-news-radar`
- `com.73wiki.interactive-qa`
- `com.73wiki.local-werss`
- `com.73wiki.tdxrs-auction-snapshot`
- `com.73wiki.tdxrs-close-basics`
- `com.73wiki.weknora-lite`

当前已正常产出的 Mac 自动数据：

- 同花顺热榜 Top100
- tdxrs 收盘基础数据
- 通达信成交额 Top100
- 互动问答
- 22点30持仓与计划票线索
- WeRSS/公众号总线仍由 15 分钟任务承接

今天是 2026-07-04 周六，竞价弱转强、交易日事实层、龙虾/老虎收盘数据缺文件属于预期，不作为故障。

## 仍需观察

| 项目 | 状态 | 处理 |
|---|---|---|
| `.conflicts/mac-migration-quarantine-20260704/windows-scripts` | 42M | 观察 7 个交易日后无引用可删 |
| `.llm-wiki/.llm-wiki` | 13M | 旧嵌套运行产物，不进索引，稳定后可删 |
| `data/data` | 746M | 旧训练数据嵌套副本，需确认外层 `data/` 完整后再删 |
| 安全 Wiki Lint | 782 个失效链接 | 不是迁移故障，后续按目录分批修 |

## 后续规则

1. Mac 主链路只能用 `.sh`、Python、Node、LaunchAgent。
2. Windows E 盘路径只允许出现在发给龙虾/老虎的 Syncthing 或回传模板里。
3. `raw/04-行情市场数据` 只作为历史文字出现，不再作为程序输入目录。
4. 全库深度 lint 必须遵守 `.wikiignore`，不要扫 `.conflicts`、`raw`、`data`、`.system`、`.llm-wiki`、`.stversions`。
5. 每次系统迁移、同步异常、lint 卡死后，先运行：

```bash
python3 raw/07-系统脚本/codex_audit_mac_migration_residue.py --write
python3 raw/07-系统脚本/codex_safe_wiki_lint.py --write
python3 raw/07-系统脚本/codex_data_interface_control.py --date YYYY-MM-DD --write
```
